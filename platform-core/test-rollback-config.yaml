apiVersion: v1
kind: ConfigMap
metadata:
  name: test-rollback-config
  namespace: odp-platform
  labels:
    app: rollback-test
    purpose: validation
data:
  version: "v2-broken"
  description: "Breaking change for rollback testing"
  test_status: "degraded"
  error: "Intentional failure to simulate production issue"

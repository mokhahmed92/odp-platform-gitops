apiVersion: v1
kind: ConfigMap
metadata:
  name: test-rollback-config
  namespace: odp-platform
  labels:
    app: rollback-test
    purpose: validation
data:
  version: "v1-commitA"
  description: "Commit A - First sequential change"
  test_status: "healthy"

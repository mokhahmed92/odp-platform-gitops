apiVersion: v1
kind: ConfigMap
metadata:
  name: test-rollback-config
  namespace: odp-platform
  labels:
    app: rollback-test
    purpose: validation
data:
  version: "v2-commitB-BROKEN"
  description: "Commit B - INTRODUCES BUG (middle commit)"
  test_status: "degraded"
  bug: "This is the problematic middle commit"

apiVersion: v1
kind: ConfigMap
metadata:
  name: test-rollback-config
  namespace: odp-platform
  labels:
    app: rollback-test
    purpose: validation
data:
  version: "v3-commitC-fixed"
  description: "Commit C with B's bug reverted - conflict resolved"
  test_status: "healthy"
  feature: "New feature added in commit C"

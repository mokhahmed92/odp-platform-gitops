apiVersion: v1
kind: ConfigMap
metadata:
  name: test-rollback-config
  namespace: odp-platform
  labels:
    app: rollback-test
    purpose: validation
data:
  version: "v3-commitC"
  description: "Commit C - Latest change (depends on B)"
  test_status: "healthy"
  bug: "This is the problematic middle commit"
  feature: "New feature added in commit C"
